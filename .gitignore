# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# server local files (databases, storage, test data)
# Database files
server/*.db
server/*.db-*
server/*.sqlite
server/*.sqlite-*

# User documents and content (keep .gitkeep to maintain directory structure)
server/documents/**
!server/documents/.gitkeep

# User uploads (images, attachments, etc.)
server/uploads/**

# Keep server README.md (documentation)
!server/README.md
