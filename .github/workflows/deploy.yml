name: Deploy on push to main
on:
  push:
    branches: [main]

env:
  APP_DIR: /home/joe/apps/text-editor
  SERVICE_NAME: text-editor

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout (metadata only)
        uses: actions/checkout@v4

      - name: Deploy over SSH
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          envs: APP_DIR,SERVICE_NAME
          script_stop: true
          script: |
            set -euo pipefail
            APP_DIR="${APP_DIR:-/home/joe/apps/text-editor}"
            SERVICE_NAME="${SERVICE_NAME:-text-editor}"
            cd "$APP_DIR"

            # Ensure repo is up to date
            if [ ! -d .git ]; then
              echo "Error: $APP_DIR is not a git repo" >&2
              exit 1
            fi
            git fetch origin
            git reset --hard origin/main

            # Install and build
            # corepack enable skipped to avoid EACCES on symlink in /usr/bin
            if ! command -v pnpm >/dev/null 2>&1; then
              echo "pnpm not found on server; please install pnpm" >&2
              exit 1
            fi
<<<<<<< HEAD
            # Install dependencies (allow build scripts for native modules)
            pnpm install --frozen-lockfile --ignore-scripts=false
            # Rebuild better-sqlite3 to ensure native module is compiled
            pnpm rebuild better-sqlite3 || echo "Warning: better-sqlite3 rebuild failed, continuing..."
=======
            # Clean any existing build artifacts and locks
            rm -rf .next .next.lock 2>/dev/null || true

            # Allow better-sqlite3 install scripts for native binding
            pnpm config set allow-scripts better-sqlite3 --location project

            # Install deps with scripts enabled so native modules compile
            pnpm install --frozen-lockfile --ignore-scripts=false

            # Rebuild better-sqlite3 to ensure native module is present
            pnpm rebuild better-sqlite3 || true

>>>>>>> a10a0eb (ci: make deployment robust for better-sqlite3; clean .next lock; skip corepack; allow scripts; rebuild native)
            pnpm build

            # Restart service (try system service, then user service)
            if command -v systemctl >/dev/null 2>&1; then
              if sudo -n systemctl status "$SERVICE_NAME" >/dev/null 2>&1; then
                echo "Restarting system service $SERVICE_NAME"
                sudo -n systemctl restart "$SERVICE_NAME"
              elif systemctl --user status "$SERVICE_NAME" >/dev/null 2>&1; then
                echo "Restarting user service $SERVICE_NAME"
                systemctl --user restart "$SERVICE_NAME"
              else
                echo "Warning: service $SERVICE_NAME not found. Skipping restart." >&2
              fi
            else
              echo "systemctl not found; skipping restart" >&2
            fi
